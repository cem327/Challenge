package org.cem.repository;

import org.cem.entity.Cart_Order;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Cart_OrderRepository extends JpaRepository<Cart_Order, Long> {
}
